# Slot-C---FOC
# include <stdio.h>
void main()
{
    int i,j,n,sum=0;
    printf("Enter a number:");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        int x=1;
        for(j=1;j<=i;j++)
        {
            x = x*i;
        }
        sum=sum+x;
    }
    printf("%d",sum);
}
