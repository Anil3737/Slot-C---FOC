# Slot-C---FOC
# include <stdio.h>
void main()
{
    int n,i,p,sum=0;
    printf("Enter the number:");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        p=i%2;
        if(p==0)
            sum=sum - i;
        else
        sum = sum+i;
        
    }
    printf("%d",sum);
}
