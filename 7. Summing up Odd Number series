# Slot-C---FOC
#include <stdio.h>
void main()
{
    int n,i,sum=0;
    printf("Enter the number: ");
    scanf("%d", &n);
    for(i=1;i<=n;i=i+2)
    {
        sum = sum+i;
        
    }
    printf("%d",sum);
}
